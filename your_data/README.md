# your_data

This folder is the default workspace-local destination for user-facing artifacts created by OctaMind.

Examples:
- generated stock reports
- zip archives created by the files agent
- downloaded email attachments
- Drive exports and downloads
- generated LinkedIn images

This directory is git-ignored except for this README so generated files stay local to each machine.